import { templateMetadata } from "../metadata/templateMetadata.js";

export const templateRegistry = {
  "saas-landing": templateMetadata["saas-landing"],
  "agency-landing": templateMetadata["agency-landing"],
  "product-launch": templateMetadata["product-launch"],
  "case-study": templateMetadata["case-study"],
};
